import { SexToCharPipe } from './sex-to-char.pipe';

describe('SexToCharPipe', () => {
  it('create an instance', () => {
    const pipe = new SexToCharPipe();
    expect(pipe).toBeTruthy();
  });
});
